# AGENTS.md

The feature list is the scope surface: its single `in-progress` entry is
the assigned task, and statuses move only with evidence.

## Work rules

- Verification: ./verify.sh --feature <id>
- Record every status change in feature_list.json.
